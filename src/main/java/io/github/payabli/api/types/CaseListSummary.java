/**
 * This file was auto-generated by Fern from our API Definition.
 */
package io.github.payabli.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import io.github.payabli.api.core.Nullable;
import io.github.payabli.api.core.NullableNonemptyFilter;
import io.github.payabli.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CaseListSummary.Builder.class)
public final class CaseListSummary {
    private final int totalRecords;

    private final double totalAmount;

    private final double totalNetAmount;

    private final int totalPages;

    private final int pageSize;

    private final Optional<String> pageidentifier;

    private final Map<String, Object> additionalProperties;

    private CaseListSummary(
            int totalRecords,
            double totalAmount,
            double totalNetAmount,
            int totalPages,
            int pageSize,
            Optional<String> pageidentifier,
            Map<String, Object> additionalProperties) {
        this.totalRecords = totalRecords;
        this.totalAmount = totalAmount;
        this.totalNetAmount = totalNetAmount;
        this.totalPages = totalPages;
        this.pageSize = pageSize;
        this.pageidentifier = pageidentifier;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The total number of matching cases.
     */
    @JsonProperty("totalRecords")
    public int getTotalRecords() {
        return totalRecords;
    }

    /**
     * @return Not used for cases; returned as part of the shared list envelope.
     */
    @JsonProperty("totalAmount")
    public double getTotalAmount() {
        return totalAmount;
    }

    /**
     * @return Not used for cases; returned as part of the shared list envelope.
     */
    @JsonProperty("totalNetAmount")
    public double getTotalNetAmount() {
        return totalNetAmount;
    }

    /**
     * @return The total number of pages.
     */
    @JsonProperty("totalPages")
    public int getTotalPages() {
        return totalPages;
    }

    /**
     * @return The number of records per page.
     */
    @JsonProperty("pageSize")
    public int getPageSize() {
        return pageSize;
    }

    /**
     * @return An opaque page identifier, when present.
     */
    @JsonIgnore
    public Optional<String> getPageidentifier() {
        if (pageidentifier == null) {
            return Optional.empty();
        }
        return pageidentifier;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("pageidentifier")
    private Optional<String> _getPageidentifier() {
        return pageidentifier;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CaseListSummary && equalTo((CaseListSummary) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CaseListSummary other) {
        return totalRecords == other.totalRecords
                && totalAmount == other.totalAmount
                && totalNetAmount == other.totalNetAmount
                && totalPages == other.totalPages
                && pageSize == other.pageSize
                && pageidentifier.equals(other.pageidentifier);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.totalRecords,
                this.totalAmount,
                this.totalNetAmount,
                this.totalPages,
                this.pageSize,
                this.pageidentifier);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static TotalRecordsStage builder() {
        return new Builder();
    }

    public interface TotalRecordsStage {
        /**
         * <p>The total number of matching cases.</p>
         */
        TotalAmountStage totalRecords(int totalRecords);

        Builder from(CaseListSummary other);
    }

    public interface TotalAmountStage {
        /**
         * <p>Not used for cases; returned as part of the shared list envelope.</p>
         */
        TotalNetAmountStage totalAmount(double totalAmount);
    }

    public interface TotalNetAmountStage {
        /**
         * <p>Not used for cases; returned as part of the shared list envelope.</p>
         */
        TotalPagesStage totalNetAmount(double totalNetAmount);
    }

    public interface TotalPagesStage {
        /**
         * <p>The total number of pages.</p>
         */
        PageSizeStage totalPages(int totalPages);
    }

    public interface PageSizeStage {
        /**
         * <p>The number of records per page.</p>
         */
        _FinalStage pageSize(int pageSize);
    }

    public interface _FinalStage {
        CaseListSummary build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>An opaque page identifier, when present.</p>
         */
        _FinalStage pageidentifier(Optional<String> pageidentifier);

        _FinalStage pageidentifier(String pageidentifier);

        _FinalStage pageidentifier(Nullable<String> pageidentifier);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements TotalRecordsStage,
                    TotalAmountStage,
                    TotalNetAmountStage,
                    TotalPagesStage,
                    PageSizeStage,
                    _FinalStage {
        private int totalRecords;

        private double totalAmount;

        private double totalNetAmount;

        private int totalPages;

        private int pageSize;

        private Optional<String> pageidentifier = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CaseListSummary other) {
            totalRecords(other.getTotalRecords());
            totalAmount(other.getTotalAmount());
            totalNetAmount(other.getTotalNetAmount());
            totalPages(other.getTotalPages());
            pageSize(other.getPageSize());
            pageidentifier(other.getPageidentifier());
            return this;
        }

        /**
         * <p>The total number of matching cases.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("totalRecords")
        public TotalAmountStage totalRecords(int totalRecords) {
            this.totalRecords = totalRecords;
            return this;
        }

        /**
         * <p>Not used for cases; returned as part of the shared list envelope.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("totalAmount")
        public TotalNetAmountStage totalAmount(double totalAmount) {
            this.totalAmount = totalAmount;
            return this;
        }

        /**
         * <p>Not used for cases; returned as part of the shared list envelope.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("totalNetAmount")
        public TotalPagesStage totalNetAmount(double totalNetAmount) {
            this.totalNetAmount = totalNetAmount;
            return this;
        }

        /**
         * <p>The total number of pages.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("totalPages")
        public PageSizeStage totalPages(int totalPages) {
            this.totalPages = totalPages;
            return this;
        }

        /**
         * <p>The number of records per page.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("pageSize")
        public _FinalStage pageSize(int pageSize) {
            this.pageSize = pageSize;
            return this;
        }

        /**
         * <p>An opaque page identifier, when present.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage pageidentifier(Nullable<String> pageidentifier) {
            if (pageidentifier.isNull()) {
                this.pageidentifier = null;
            } else if (pageidentifier.isEmpty()) {
                this.pageidentifier = Optional.empty();
            } else {
                this.pageidentifier = Optional.of(pageidentifier.get());
            }
            return this;
        }

        /**
         * <p>An opaque page identifier, when present.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage pageidentifier(String pageidentifier) {
            this.pageidentifier = Optional.ofNullable(pageidentifier);
            return this;
        }

        /**
         * <p>An opaque page identifier, when present.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "pageidentifier", nulls = Nulls.SKIP)
        public _FinalStage pageidentifier(Optional<String> pageidentifier) {
            this.pageidentifier = pageidentifier;
            return this;
        }

        @java.lang.Override
        public CaseListSummary build() {
            return new CaseListSummary(
                    totalRecords,
                    totalAmount,
                    totalNetAmount,
                    totalPages,
                    pageSize,
                    pageidentifier,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}

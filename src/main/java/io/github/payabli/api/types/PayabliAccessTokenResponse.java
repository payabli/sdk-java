/**
 * This file was auto-generated by Fern from our API Definition.
 */
package io.github.payabli.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import io.github.payabli.api.core.Nullable;
import io.github.payabli.api.core.NullableNonemptyFilter;
import io.github.payabli.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = PayabliAccessTokenResponse.Builder.class)
public final class PayabliAccessTokenResponse {
    private final String tokenType;

    private final String accessToken;

    private final int expiresIn;

    private final Optional<String> state;

    private final Map<String, Object> additionalProperties;

    private PayabliAccessTokenResponse(
            String tokenType,
            String accessToken,
            int expiresIn,
            Optional<String> state,
            Map<String, Object> additionalProperties) {
        this.tokenType = tokenType;
        this.accessToken = accessToken;
        this.expiresIn = expiresIn;
        this.state = state;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The token type. Send the access token in the <code>Authorization</code> header as <code>Bearer &lt;access_token&gt;</code>.
     */
    @JsonProperty("token_type")
    public String getTokenType() {
        return tokenType;
    }

    /**
     * @return The access token to send on subsequent API calls.
     */
    @JsonProperty("access_token")
    public String getAccessToken() {
        return accessToken;
    }

    /**
     * @return The token's lifetime in seconds. Request a new token when it expires.
     */
    @JsonProperty("expires_in")
    public int getExpiresIn() {
        return expiresIn;
    }

    /**
     * @return The opaque value sent in the request, echoed back. Present only when you send <code>state</code> in the request.
     */
    @JsonIgnore
    public Optional<String> getState() {
        if (state == null) {
            return Optional.empty();
        }
        return state;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("state")
    private Optional<String> _getState() {
        return state;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof PayabliAccessTokenResponse && equalTo((PayabliAccessTokenResponse) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(PayabliAccessTokenResponse other) {
        return tokenType.equals(other.tokenType)
                && accessToken.equals(other.accessToken)
                && expiresIn == other.expiresIn
                && state.equals(other.state);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.tokenType, this.accessToken, this.expiresIn, this.state);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static TokenTypeStage builder() {
        return new Builder();
    }

    public interface TokenTypeStage {
        /**
         * <p>The token type. Send the access token in the <code>Authorization</code> header as <code>Bearer &lt;access_token&gt;</code>.</p>
         */
        AccessTokenStage tokenType(@NotNull String tokenType);

        Builder from(PayabliAccessTokenResponse other);
    }

    public interface AccessTokenStage {
        /**
         * <p>The access token to send on subsequent API calls.</p>
         */
        ExpiresInStage accessToken(@NotNull String accessToken);
    }

    public interface ExpiresInStage {
        /**
         * <p>The token's lifetime in seconds. Request a new token when it expires.</p>
         */
        _FinalStage expiresIn(int expiresIn);
    }

    public interface _FinalStage {
        PayabliAccessTokenResponse build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p>The opaque value sent in the request, echoed back. Present only when you send <code>state</code> in the request.</p>
         */
        _FinalStage state(Optional<String> state);

        _FinalStage state(String state);

        _FinalStage state(Nullable<String> state);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements TokenTypeStage, AccessTokenStage, ExpiresInStage, _FinalStage {
        private String tokenType;

        private String accessToken;

        private int expiresIn;

        private Optional<String> state = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(PayabliAccessTokenResponse other) {
            tokenType(other.getTokenType());
            accessToken(other.getAccessToken());
            expiresIn(other.getExpiresIn());
            state(other.getState());
            return this;
        }

        /**
         * <p>The token type. Send the access token in the <code>Authorization</code> header as <code>Bearer &lt;access_token&gt;</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("token_type")
        public AccessTokenStage tokenType(@NotNull String tokenType) {
            this.tokenType = Objects.requireNonNull(tokenType, "tokenType must not be null");
            return this;
        }

        /**
         * <p>The access token to send on subsequent API calls.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("access_token")
        public ExpiresInStage accessToken(@NotNull String accessToken) {
            this.accessToken = Objects.requireNonNull(accessToken, "accessToken must not be null");
            return this;
        }

        /**
         * <p>The token's lifetime in seconds. Request a new token when it expires.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("expires_in")
        public _FinalStage expiresIn(int expiresIn) {
            this.expiresIn = expiresIn;
            return this;
        }

        /**
         * <p>The opaque value sent in the request, echoed back. Present only when you send <code>state</code> in the request.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage state(Nullable<String> state) {
            if (state.isNull()) {
                this.state = null;
            } else if (state.isEmpty()) {
                this.state = Optional.empty();
            } else {
                this.state = Optional.of(state.get());
            }
            return this;
        }

        /**
         * <p>The opaque value sent in the request, echoed back. Present only when you send <code>state</code> in the request.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage state(String state) {
            this.state = Optional.ofNullable(state);
            return this;
        }

        /**
         * <p>The opaque value sent in the request, echoed back. Present only when you send <code>state</code> in the request.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "state", nulls = Nulls.SKIP)
        public _FinalStage state(Optional<String> state) {
            this.state = state;
            return this;
        }

        @java.lang.Override
        public PayabliAccessTokenResponse build() {
            return new PayabliAccessTokenResponse(tokenType, accessToken, expiresIn, state, additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}

/**
 * This file was auto-generated by Fern from our API Definition.
 */
package io.github.payabli.api.resources.management;

import io.github.payabli.api.core.ClientOptions;
import io.github.payabli.api.core.RequestOptions;
import io.github.payabli.api.resources.management.requests.VerifyAccountDetailsRequest;
import io.github.payabli.api.resources.management.types.VerifyAccountDetailsResponse;
import java.util.concurrent.CompletableFuture;

public class AsyncManagementClient {
    protected final ClientOptions clientOptions;

    private final AsyncRawManagementClient rawClient;

    public AsyncManagementClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new AsyncRawManagementClient(clientOptions);
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public AsyncRawManagementClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * Verifies a bank account and returns detailed verification results from the verification network, including bank name, account status, and response codes. Unlike a pass/fail verification, this endpoint returns granular data to support decision-making and troubleshooting.
     * <p>When bank authentication is enabled for the paypoint's organization, the endpoint performs an identity verification check on the account holder. Otherwise, it performs an account existence check. When bank authentication is enabled, the <code>accountHolderType</code> and <code>holderName</code> fields are required.</p>
     * <p>Requires <code>inboundpayments_create</code> or <code>outboundpayments_create</code> permission.</p>
     */
    public CompletableFuture<VerifyAccountDetailsResponse> verifyAccountDetails(
            String entry, VerifyAccountDetailsRequest request) {
        return this.rawClient.verifyAccountDetails(entry, request).thenApply(response -> response.body());
    }

    /**
     * Verifies a bank account and returns detailed verification results from the verification network, including bank name, account status, and response codes. Unlike a pass/fail verification, this endpoint returns granular data to support decision-making and troubleshooting.
     * <p>When bank authentication is enabled for the paypoint's organization, the endpoint performs an identity verification check on the account holder. Otherwise, it performs an account existence check. When bank authentication is enabled, the <code>accountHolderType</code> and <code>holderName</code> fields are required.</p>
     * <p>Requires <code>inboundpayments_create</code> or <code>outboundpayments_create</code> permission.</p>
     */
    public CompletableFuture<VerifyAccountDetailsResponse> verifyAccountDetails(
            String entry, VerifyAccountDetailsRequest request, RequestOptions requestOptions) {
        return this.rawClient
                .verifyAccountDetails(entry, request, requestOptions)
                .thenApply(response -> response.body());
    }
}

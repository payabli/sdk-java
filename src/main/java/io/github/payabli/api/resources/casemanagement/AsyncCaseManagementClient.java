/**
 * This file was auto-generated by Fern from our API Definition.
 */
package io.github.payabli.api.resources.casemanagement;

import io.github.payabli.api.core.ClientOptions;
import io.github.payabli.api.core.RequestOptions;
import io.github.payabli.api.resources.casemanagement.requests.AssignCaseRequest;
import io.github.payabli.api.resources.casemanagement.requests.CreateBankAccountChangeCaseRequest;
import io.github.payabli.api.resources.casemanagement.requests.ListCasesCaseManagementRequest;
import io.github.payabli.api.resources.casemanagement.requests.ListMessagesCaseManagementRequest;
import io.github.payabli.api.resources.casemanagement.requests.PostCaseMessageRequest;
import io.github.payabli.api.resources.casemanagement.requests.TransitionCaseRequest;
import io.github.payabli.api.resources.casemanagement.requests.UploadAttachmentCaseManagementRequest;
import io.github.payabli.api.resources.casemanagement.requests.ValidateBankAccountChangeRequest;
import io.github.payabli.api.types.AttachmentResponse;
import io.github.payabli.api.types.AvailableTransitionsResponse;
import io.github.payabli.api.types.CaseListResponse;
import io.github.payabli.api.types.CaseResponse;
import io.github.payabli.api.types.MessagePage;
import io.github.payabli.api.types.PostedMessage;
import io.github.payabli.api.types.PreCreationValidationResult;
import java.io.File;
import java.io.InputStream;
import java.util.List;
import java.util.concurrent.CompletableFuture;
import okhttp3.MediaType;

public class AsyncCaseManagementClient {
    protected final ClientOptions clientOptions;

    private final AsyncRawCaseManagementClient rawClient;

    public AsyncCaseManagementClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new AsyncRawCaseManagementClient(clientOptions);
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public AsyncRawCaseManagementClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * Validates a bank account change for a paypoint without creating a case.
     * Runs the same checks the create endpoint runs, and returns blocking
     * conditions and warnings. Blocking conditions prevent creation; warnings
     * don't.
     * <p>Available to both Platform and Enterprise Partners.</p>
     */
    public CompletableFuture<PreCreationValidationResult> validateBankAccountChange(
            long paypointId, ValidateBankAccountChangeRequest request) {
        return this.rawClient.validateBankAccountChange(paypointId, request).thenApply(response -> response.body());
    }

    /**
     * Validates a bank account change for a paypoint without creating a case.
     * Runs the same checks the create endpoint runs, and returns blocking
     * conditions and warnings. Blocking conditions prevent creation; warnings
     * don't.
     * <p>Available to both Platform and Enterprise Partners.</p>
     */
    public CompletableFuture<PreCreationValidationResult> validateBankAccountChange(
            long paypointId, ValidateBankAccountChangeRequest request, RequestOptions requestOptions) {
        return this.rawClient
                .validateBankAccountChange(paypointId, request, requestOptions)
                .thenApply(response -> response.body());
    }

    /**
     * Creates a bank-account-change case for a paypoint. The account and
     * routing numbers are validated and tokenized before the case is saved —
     * the raw numbers are never stored or returned. The account holder name is
     * taken from the paypoint's legal name. On success the case is created in
     * <code>Submitted</code> and asynchronous verification starts.
     * <p>Available to both Platform and Enterprise Partners.</p>
     */
    public CompletableFuture<CaseResponse> createBankAccountChange(
            long paypointId, CreateBankAccountChangeCaseRequest request) {
        return this.rawClient.createBankAccountChange(paypointId, request).thenApply(response -> response.body());
    }

    /**
     * Creates a bank-account-change case for a paypoint. The account and
     * routing numbers are validated and tokenized before the case is saved —
     * the raw numbers are never stored or returned. The account holder name is
     * taken from the paypoint's legal name. On success the case is created in
     * <code>Submitted</code> and asynchronous verification starts.
     * <p>Available to both Platform and Enterprise Partners.</p>
     */
    public CompletableFuture<CaseResponse> createBankAccountChange(
            long paypointId, CreateBankAccountChangeCaseRequest request, RequestOptions requestOptions) {
        return this.rawClient
                .createBankAccountChange(paypointId, request, requestOptions)
                .thenApply(response -> response.body());
    }

    /**
     * Returns a case by its UUID, including its current state, parameters,
     * state history, verification metadata, and attachments.
     * <p>Available to both Platform and Enterprise Partners.</p>
     */
    public CompletableFuture<CaseResponse> getCase(String uuid) {
        return this.rawClient.getCase(uuid).thenApply(response -> response.body());
    }

    /**
     * Returns a case by its UUID, including its current state, parameters,
     * state history, verification metadata, and attachments.
     * <p>Available to both Platform and Enterprise Partners.</p>
     */
    public CompletableFuture<CaseResponse> getCase(String uuid, RequestOptions requestOptions) {
        return this.rawClient.getCase(uuid, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Lists cases for an organization, climbing the platform org hierarchy.
     * Supports pagination and sorting through query parameters, and filtering
     * through repeatable <code>parameters[field(op)]=value</code> query parameters (for
     * example <code>parameters[state(in)]=Assigned|PendingReview</code>). Filterable
     * fields include <code>state</code>, <code>caseType</code>, <code>paypointId</code>, <code>createdAt</code>,
     * <code>updatedAt</code>, <code>scheduleFor</code>, and <code>createdBy</code>.
     * <p>Available to both Platform and Enterprise Partners.</p>
     */
    public CompletableFuture<CaseListResponse> listCases(long organizationId) {
        return this.rawClient.listCases(organizationId).thenApply(response -> response.body());
    }

    /**
     * Lists cases for an organization, climbing the platform org hierarchy.
     * Supports pagination and sorting through query parameters, and filtering
     * through repeatable <code>parameters[field(op)]=value</code> query parameters (for
     * example <code>parameters[state(in)]=Assigned|PendingReview</code>). Filterable
     * fields include <code>state</code>, <code>caseType</code>, <code>paypointId</code>, <code>createdAt</code>,
     * <code>updatedAt</code>, <code>scheduleFor</code>, and <code>createdBy</code>.
     * <p>Available to both Platform and Enterprise Partners.</p>
     */
    public CompletableFuture<CaseListResponse> listCases(long organizationId, RequestOptions requestOptions) {
        return this.rawClient.listCases(organizationId, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Lists cases for an organization, climbing the platform org hierarchy.
     * Supports pagination and sorting through query parameters, and filtering
     * through repeatable <code>parameters[field(op)]=value</code> query parameters (for
     * example <code>parameters[state(in)]=Assigned|PendingReview</code>). Filterable
     * fields include <code>state</code>, <code>caseType</code>, <code>paypointId</code>, <code>createdAt</code>,
     * <code>updatedAt</code>, <code>scheduleFor</code>, and <code>createdBy</code>.
     * <p>Available to both Platform and Enterprise Partners.</p>
     */
    public CompletableFuture<CaseListResponse> listCases(long organizationId, ListCasesCaseManagementRequest request) {
        return this.rawClient.listCases(organizationId, request).thenApply(response -> response.body());
    }

    /**
     * Lists cases for an organization, climbing the platform org hierarchy.
     * Supports pagination and sorting through query parameters, and filtering
     * through repeatable <code>parameters[field(op)]=value</code> query parameters (for
     * example <code>parameters[state(in)]=Assigned|PendingReview</code>). Filterable
     * fields include <code>state</code>, <code>caseType</code>, <code>paypointId</code>, <code>createdAt</code>,
     * <code>updatedAt</code>, <code>scheduleFor</code>, and <code>createdBy</code>.
     * <p>Available to both Platform and Enterprise Partners.</p>
     */
    public CompletableFuture<CaseListResponse> listCases(
            long organizationId, ListCasesCaseManagementRequest request, RequestOptions requestOptions) {
        return this.rawClient.listCases(organizationId, request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Lists the notes on a case, ordered oldest to newest. Cursor-paginated.
     * <p>Available to both Platform and Enterprise Partners.</p>
     */
    public CompletableFuture<MessagePage> listMessages(String caseUuid) {
        return this.rawClient.listMessages(caseUuid).thenApply(response -> response.body());
    }

    /**
     * Lists the notes on a case, ordered oldest to newest. Cursor-paginated.
     * <p>Available to both Platform and Enterprise Partners.</p>
     */
    public CompletableFuture<MessagePage> listMessages(String caseUuid, RequestOptions requestOptions) {
        return this.rawClient.listMessages(caseUuid, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Lists the notes on a case, ordered oldest to newest. Cursor-paginated.
     * <p>Available to both Platform and Enterprise Partners.</p>
     */
    public CompletableFuture<MessagePage> listMessages(String caseUuid, ListMessagesCaseManagementRequest request) {
        return this.rawClient.listMessages(caseUuid, request).thenApply(response -> response.body());
    }

    /**
     * Lists the notes on a case, ordered oldest to newest. Cursor-paginated.
     * <p>Available to both Platform and Enterprise Partners.</p>
     */
    public CompletableFuture<MessagePage> listMessages(
            String caseUuid, ListMessagesCaseManagementRequest request, RequestOptions requestOptions) {
        return this.rawClient.listMessages(caseUuid, request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Adds a note to a case.
     * <p>Available to both Platform and Enterprise Partners.</p>
     * <p>This endpoint is in development and not yet available for API use. To
     * add a note for now, use Case Management in the
     * <a href="/guides/pay-ops-portal-bank-account-changes-manage">Payabli Portal</a>.
     * To read existing notes on a case, use
     * <a href="/developers/api-reference/caseManagement/list-case-notes">List case notes</a>.</p>
     */
    public CompletableFuture<PostedMessage> postMessage(String caseUuid, PostCaseMessageRequest request) {
        return this.rawClient.postMessage(caseUuid, request).thenApply(response -> response.body());
    }

    /**
     * Adds a note to a case.
     * <p>Available to both Platform and Enterprise Partners.</p>
     * <p>This endpoint is in development and not yet available for API use. To
     * add a note for now, use Case Management in the
     * <a href="/guides/pay-ops-portal-bank-account-changes-manage">Payabli Portal</a>.
     * To read existing notes on a case, use
     * <a href="/developers/api-reference/caseManagement/list-case-notes">List case notes</a>.</p>
     */
    public CompletableFuture<PostedMessage> postMessage(
            String caseUuid, PostCaseMessageRequest request, RequestOptions requestOptions) {
        return this.rawClient.postMessage(caseUuid, request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Lists the review actions currently available on a case. The list is
     * empty when no user action is available (for example while the case is
     * mid-automation).
     * <p>Available to both Platform and Enterprise Partners, though only
     * Enterprise Partners can fire the returned actions.</p>
     */
    public CompletableFuture<AvailableTransitionsResponse> listTransitions(String uuid) {
        return this.rawClient.listTransitions(uuid).thenApply(response -> response.body());
    }

    /**
     * Lists the review actions currently available on a case. The list is
     * empty when no user action is available (for example while the case is
     * mid-automation).
     * <p>Available to both Platform and Enterprise Partners, though only
     * Enterprise Partners can fire the returned actions.</p>
     */
    public CompletableFuture<AvailableTransitionsResponse> listTransitions(String uuid, RequestOptions requestOptions) {
        return this.rawClient.listTransitions(uuid, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Fires a review action on a case, such as <code>Approve</code>, <code>Deny</code>, <code>Escalate</code>,
     * or <code>RequestReview</code>. Assigning a case uses the dedicated assign endpoint,
     * not this one. Firing an action that isn't valid for the case's current
     * state returns <code>409</code>.
     * <p>Available to Enterprise Partners only.</p>
     */
    public CompletableFuture<CaseResponse> transition(String uuid, TransitionCaseRequest request) {
        return this.rawClient.transition(uuid, request).thenApply(response -> response.body());
    }

    /**
     * Fires a review action on a case, such as <code>Approve</code>, <code>Deny</code>, <code>Escalate</code>,
     * or <code>RequestReview</code>. Assigning a case uses the dedicated assign endpoint,
     * not this one. Firing an action that isn't valid for the case's current
     * state returns <code>409</code>.
     * <p>Available to Enterprise Partners only.</p>
     */
    public CompletableFuture<CaseResponse> transition(
            String uuid, TransitionCaseRequest request, RequestOptions requestOptions) {
        return this.rawClient.transition(uuid, request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Assigns a case to a reviewer.
     * <p>Available to Enterprise Partners only.</p>
     */
    public CompletableFuture<CaseResponse> assignCase(String uuid, AssignCaseRequest request) {
        return this.rawClient.assignCase(uuid, request).thenApply(response -> response.body());
    }

    /**
     * Assigns a case to a reviewer.
     * <p>Available to Enterprise Partners only.</p>
     */
    public CompletableFuture<CaseResponse> assignCase(
            String uuid, AssignCaseRequest request, RequestOptions requestOptions) {
        return this.rawClient.assignCase(uuid, request, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Lists the files attached to a case.
     * <p>Available to both Platform and Enterprise Partners.</p>
     */
    public CompletableFuture<List<AttachmentResponse>> listAttachments(String caseUuid) {
        return this.rawClient.listAttachments(caseUuid).thenApply(response -> response.body());
    }

    /**
     * Lists the files attached to a case.
     * <p>Available to both Platform and Enterprise Partners.</p>
     */
    public CompletableFuture<List<AttachmentResponse>> listAttachments(String caseUuid, RequestOptions requestOptions) {
        return this.rawClient.listAttachments(caseUuid, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Uploads a file to a case as multipart form data. The maximum size is
     * 25 MiB, and the content type must be an allowed type such as PDF, PNG,
     * JPEG, CSV, XLSX, DOCX, or plain text.
     * <p>Available to both Platform and Enterprise Partners.</p>
     */
    public CompletableFuture<AttachmentResponse> uploadAttachment(
            String caseUuid, File file, UploadAttachmentCaseManagementRequest request) {
        return this.rawClient.uploadAttachment(caseUuid, file, request).thenApply(response -> response.body());
    }

    /**
     * Uploads a file to a case as multipart form data. The maximum size is
     * 25 MiB, and the content type must be an allowed type such as PDF, PNG,
     * JPEG, CSV, XLSX, DOCX, or plain text.
     * <p>Available to both Platform and Enterprise Partners.</p>
     */
    public CompletableFuture<AttachmentResponse> uploadAttachment(
            String caseUuid, File file, UploadAttachmentCaseManagementRequest request, RequestOptions requestOptions) {
        return this.rawClient
                .uploadAttachment(caseUuid, file, request, requestOptions)
                .thenApply(response -> response.body());
    }

    public CompletableFuture<AttachmentResponse> uploadAttachment(
            String caseUuid, InputStream stream, String filename) {
        return this.rawClient.uploadAttachment(caseUuid, stream, filename).thenApply(response -> response.body());
    }

    public CompletableFuture<AttachmentResponse> uploadAttachment(
            String caseUuid, InputStream stream, String filename, MediaType mediaType) {
        return this.rawClient
                .uploadAttachment(caseUuid, stream, filename, mediaType)
                .thenApply(response -> response.body());
    }

    public CompletableFuture<AttachmentResponse> uploadAttachment(
            String caseUuid, InputStream stream, String filename, RequestOptions requestOptions) {
        return this.rawClient
                .uploadAttachment(caseUuid, stream, filename, requestOptions)
                .thenApply(response -> response.body());
    }

    public CompletableFuture<AttachmentResponse> uploadAttachment(
            String caseUuid, InputStream stream, String filename, MediaType mediaType, RequestOptions requestOptions) {
        return this.rawClient
                .uploadAttachment(caseUuid, stream, filename, mediaType, requestOptions)
                .thenApply(response -> response.body());
    }

    /**
     * Streams the file content of an attachment.
     * <p>Available to both Platform and Enterprise Partners.</p>
     */
    public CompletableFuture<InputStream> getAttachment(String caseUuid, String attachmentId) {
        return this.rawClient.getAttachment(caseUuid, attachmentId).thenApply(response -> response.body());
    }

    /**
     * Streams the file content of an attachment.
     * <p>Available to both Platform and Enterprise Partners.</p>
     */
    public CompletableFuture<InputStream> getAttachment(
            String caseUuid, String attachmentId, RequestOptions requestOptions) {
        return this.rawClient
                .getAttachment(caseUuid, attachmentId, requestOptions)
                .thenApply(response -> response.body());
    }

    /**
     * Deletes an attachment from a case.
     * <p>Available to both Platform and Enterprise Partners.</p>
     */
    public CompletableFuture<Void> deleteAttachment(String caseUuid, String attachmentId) {
        return this.rawClient.deleteAttachment(caseUuid, attachmentId).thenApply(response -> response.body());
    }

    /**
     * Deletes an attachment from a case.
     * <p>Available to both Platform and Enterprise Partners.</p>
     */
    public CompletableFuture<Void> deleteAttachment(
            String caseUuid, String attachmentId, RequestOptions requestOptions) {
        return this.rawClient
                .deleteAttachment(caseUuid, attachmentId, requestOptions)
                .thenApply(response -> response.body());
    }
}

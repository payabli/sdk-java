/**
 * This file was auto-generated by Fern from our API Definition.
 */
package io.github.payabli.api.resources.query.requests;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import io.github.payabli.api.core.ObjectMappers;
import io.github.payabli.api.types.ExportFormat;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = ListPayoutOrgRequest.Builder.class)
public final class ListPayoutOrgRequest {
    private final Optional<ExportFormat> exportFormat;

    private final Optional<Integer> fromRecord;

    private final Optional<Integer> limitRecord;

    private final Optional<Map<String, Optional<String>>> parameters;

    private final Optional<String> sortBy;

    private final Map<String, Object> additionalProperties;

    private ListPayoutOrgRequest(
            Optional<ExportFormat> exportFormat,
            Optional<Integer> fromRecord,
            Optional<Integer> limitRecord,
            Optional<Map<String, Optional<String>>> parameters,
            Optional<String> sortBy,
            Map<String, Object> additionalProperties) {
        this.exportFormat = exportFormat;
        this.fromRecord = fromRecord;
        this.limitRecord = limitRecord;
        this.parameters = parameters;
        this.sortBy = sortBy;
        this.additionalProperties = additionalProperties;
    }

    @JsonProperty("exportFormat")
    public Optional<ExportFormat> getExportFormat() {
        return exportFormat;
    }

    /**
     * @return The number of records to skip before starting to collect the result set.
     */
    @JsonProperty("fromRecord")
    public Optional<Integer> getFromRecord() {
        return fromRecord;
    }

    /**
     * @return Max number of records to return for the query. Use <code>0</code> or negative value to return all records.
     */
    @JsonProperty("limitRecord")
    public Optional<Integer> getLimitRecord() {
        return limitRecord;
    }

    /**
     * @return Collection of field names, conditions, and values used to filter the query.
     * &lt;Info&gt;
     * <strong>You must remove <code>parameters=</code> from the request before you send it, otherwise Payabli will ignore the filters.</strong>
     * <p>Because of a technical limitation, you can't make a request that includes filters from the API console on this page. The response won't be filtered. Instead, copy the request, remove <code>parameters=</code> and run the request in a different client.</p>
     * <p>For example:</p>
     * <p>--url https://api-sandbox.payabli.com/api/Query/transactions/org/236?parameters=totalAmount(gt)=1000&amp;limitRecord=20</p>
     * <p>should become:</p>
     * <p>--url https://api-sandbox.payabli.com/api/Query/transactions/org/236?totalAmount(gt)=1000&amp;limitRecord=20
     * &lt;/Info&gt;
     * List of field names accepted:</p>
     * <ul>
     * <li><code>status</code> (in, nin, eq, ne)</li>
     * <li><code>transactionDate</code> (gt, ge, lt, le, eq, ne)</li>
     * <li><code>billNumber</code> (ct, nct)</li>
     * <li><code>vendorNumber</code> (ct, nct, eq, ne)</li>
     * <li><code>vendorName</code> (ct, nct, eq, ne)</li>
     * <li><code>parentOrgId</code> (ne, eq, nin, in)</li>
     * <li><code>paymentMethod</code> (ct, nct, eq, ne, in, nin)</li>
     * <li><code>paymentId</code> (ct, nct, eq, ne)</li>
     * <li><code>batchNumber</code> (ct, nct, eq, ne)</li>
     * <li><code>totalAmount</code> (gt, ge, lt, le, eq, ne)</li>
     * <li><code>paypointLegal</code> (ne, eq, ct, nct)</li>
     * <li><code>paypointDba</code> (ne, eq, ct, nct)</li>
     * <li><code>accountId</code> (ne, eq, ct, nct)</li>
     * <li><code>orgName</code> (ne, eq, ct, nct)</li>
     * <li><code>externalPaypointID</code> (ct, nct, eq, ne)</li>
     * <li><code>paypointId</code> (eq, ne)</li>
     * <li><code>vendorId</code> (eq, ne)</li>
     * <li><code>vendorEIN</code> (ct, nct, eq, ne)</li>
     * <li><code>vendorPhone</code> (ct, nct, eq, ne)</li>
     * <li><code>vendorEmail</code> (ct, nct, eq, ne)</li>
     * <li><code>vendorAddress</code> (ct, nct, eq, ne)</li>
     * <li><code>vendorCity</code> (ct, nct, eq, ne)</li>
     * <li><code>vendorState</code> (ct, nct, eq, ne)</li>
     * <li><code>vendorCountry</code> (ct, nct, eq, ne)</li>
     * <li><code>vendorZip</code> (ct, nct, eq, ne)</li>
     * <li><code>vendorMCC</code> (ct, nct, eq, ne)</li>
     * <li><code>vendorLocationCode</code> (ct, nct, eq, ne)</li>
     * <li><code>vendorCustomField1</code> (ct, nct, eq, ne)</li>
     * <li><code>vendorCustomField2</code> (ct, nct, eq, ne)</li>
     * <li><code>comments</code> (ct, nct)</li>
     * <li><code>payaccountCurrency</code> (ne, eq, in, nin)</li>
     * <li><code>remitAddress</code> (ct, nct)</li>
     * <li><code>source</code> (ct, nct, eq, ne)</li>
     * <li><code>updatedOn</code> (gt, ge, lt, le, eq, ne)</li>
     * <li><code>feeAmount</code> (gt, ge, lt, le, eq, ne)</li>
     * <li><code>lotNumber</code> (ct, nct)</li>
     * <li><code>customerVendorAccount</code> (ct, nct, eq, ne)</li>
     * <li><code>batchId</code> (eq, ne)</li>
     * <li><code>AchTraceNumber</code> (eq, ne)</li>
     * <li><code>payoutProgram</code>(eq, ne) the options are <code>managed</code> or <code>odp</code>. For example, <code>payoutProgram(eq)=managed</code> returns all records with a <code>payoutProgram</code> equal to <code>managed</code>.</li>
     * </ul>
     * <p>List of comparison accepted - enclosed between parentheses:</p>
     * <ul>
     * <li>eq or empty =&gt; equal</li>
     * <li>gt =&gt; greater than</li>
     * <li>ge =&gt; greater or equal</li>
     * <li>lt =&gt; less than</li>
     * <li>le =&gt; less or equal</li>
     * <li>ne =&gt; not equal</li>
     * <li>ct =&gt; contains</li>
     * <li>nct =&gt; not contains</li>
     * <li>in =&gt; inside array separated by &quot;|&quot;</li>
     * <li>nin =&gt; not inside array separated by &quot;|&quot;</li>
     * </ul>
     * <p>List of parameters accepted:</p>
     * <ul>
     * <li>limitRecord : max number of records for query (default=&quot;20&quot;, &quot;0&quot; or negative value for all)</li>
     * <li>fromRecord : initial record in query</li>
     * <li>sortBy : indicate field name and direction to sort the results</li>
     * </ul>
     * <p>Example: <code>netAmount(gt)=20</code> returns all records with a <code>netAmount</code> greater than 20.00</p>
     * <p>Example: <code>sortBy=desc(netamount)</code> returns all records sorted by <code>netAmount</code> descending</p>
     */
    @JsonProperty("parameters")
    public Optional<Map<String, Optional<String>>> getParameters() {
        return parameters;
    }

    /**
     * @return The field name to use for sorting results. Use <code>desc(field_name)</code> to sort descending by <code>field_name</code>, and use <code>asc(field_name)</code> to sort ascending by <code>field_name</code>.
     */
    @JsonProperty("sortBy")
    public Optional<String> getSortBy() {
        return sortBy;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof ListPayoutOrgRequest && equalTo((ListPayoutOrgRequest) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(ListPayoutOrgRequest other) {
        return exportFormat.equals(other.exportFormat)
                && fromRecord.equals(other.fromRecord)
                && limitRecord.equals(other.limitRecord)
                && parameters.equals(other.parameters)
                && sortBy.equals(other.sortBy);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.exportFormat, this.fromRecord, this.limitRecord, this.parameters, this.sortBy);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static Builder builder() {
        return new Builder();
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder {
        private Optional<ExportFormat> exportFormat = Optional.empty();

        private Optional<Integer> fromRecord = Optional.empty();

        private Optional<Integer> limitRecord = Optional.empty();

        private Optional<Map<String, Optional<String>>> parameters = Optional.empty();

        private Optional<String> sortBy = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        public Builder from(ListPayoutOrgRequest other) {
            exportFormat(other.getExportFormat());
            fromRecord(other.getFromRecord());
            limitRecord(other.getLimitRecord());
            parameters(other.getParameters());
            sortBy(other.getSortBy());
            return this;
        }

        @JsonSetter(value = "exportFormat", nulls = Nulls.SKIP)
        public Builder exportFormat(Optional<ExportFormat> exportFormat) {
            this.exportFormat = exportFormat;
            return this;
        }

        public Builder exportFormat(ExportFormat exportFormat) {
            this.exportFormat = Optional.ofNullable(exportFormat);
            return this;
        }

        /**
         * <p>The number of records to skip before starting to collect the result set.</p>
         */
        @JsonSetter(value = "fromRecord", nulls = Nulls.SKIP)
        public Builder fromRecord(Optional<Integer> fromRecord) {
            this.fromRecord = fromRecord;
            return this;
        }

        public Builder fromRecord(Integer fromRecord) {
            this.fromRecord = Optional.ofNullable(fromRecord);
            return this;
        }

        /**
         * <p>Max number of records to return for the query. Use <code>0</code> or negative value to return all records.</p>
         */
        @JsonSetter(value = "limitRecord", nulls = Nulls.SKIP)
        public Builder limitRecord(Optional<Integer> limitRecord) {
            this.limitRecord = limitRecord;
            return this;
        }

        public Builder limitRecord(Integer limitRecord) {
            this.limitRecord = Optional.ofNullable(limitRecord);
            return this;
        }

        /**
         * <p>Collection of field names, conditions, and values used to filter the query.
         * &lt;Info&gt;
         * <strong>You must remove <code>parameters=</code> from the request before you send it, otherwise Payabli will ignore the filters.</strong></p>
         * <p>Because of a technical limitation, you can't make a request that includes filters from the API console on this page. The response won't be filtered. Instead, copy the request, remove <code>parameters=</code> and run the request in a different client.</p>
         * <p>For example:</p>
         * <p>--url https://api-sandbox.payabli.com/api/Query/transactions/org/236?parameters=totalAmount(gt)=1000&amp;limitRecord=20</p>
         * <p>should become:</p>
         * <p>--url https://api-sandbox.payabli.com/api/Query/transactions/org/236?totalAmount(gt)=1000&amp;limitRecord=20
         * &lt;/Info&gt;
         * List of field names accepted:</p>
         * <ul>
         * <li><code>status</code> (in, nin, eq, ne)</li>
         * <li><code>transactionDate</code> (gt, ge, lt, le, eq, ne)</li>
         * <li><code>billNumber</code> (ct, nct)</li>
         * <li><code>vendorNumber</code> (ct, nct, eq, ne)</li>
         * <li><code>vendorName</code> (ct, nct, eq, ne)</li>
         * <li><code>parentOrgId</code> (ne, eq, nin, in)</li>
         * <li><code>paymentMethod</code> (ct, nct, eq, ne, in, nin)</li>
         * <li><code>paymentId</code> (ct, nct, eq, ne)</li>
         * <li><code>batchNumber</code> (ct, nct, eq, ne)</li>
         * <li><code>totalAmount</code> (gt, ge, lt, le, eq, ne)</li>
         * <li><code>paypointLegal</code> (ne, eq, ct, nct)</li>
         * <li><code>paypointDba</code> (ne, eq, ct, nct)</li>
         * <li><code>accountId</code> (ne, eq, ct, nct)</li>
         * <li><code>orgName</code> (ne, eq, ct, nct)</li>
         * <li><code>externalPaypointID</code> (ct, nct, eq, ne)</li>
         * <li><code>paypointId</code> (eq, ne)</li>
         * <li><code>vendorId</code> (eq, ne)</li>
         * <li><code>vendorEIN</code> (ct, nct, eq, ne)</li>
         * <li><code>vendorPhone</code> (ct, nct, eq, ne)</li>
         * <li><code>vendorEmail</code> (ct, nct, eq, ne)</li>
         * <li><code>vendorAddress</code> (ct, nct, eq, ne)</li>
         * <li><code>vendorCity</code> (ct, nct, eq, ne)</li>
         * <li><code>vendorState</code> (ct, nct, eq, ne)</li>
         * <li><code>vendorCountry</code> (ct, nct, eq, ne)</li>
         * <li><code>vendorZip</code> (ct, nct, eq, ne)</li>
         * <li><code>vendorMCC</code> (ct, nct, eq, ne)</li>
         * <li><code>vendorLocationCode</code> (ct, nct, eq, ne)</li>
         * <li><code>vendorCustomField1</code> (ct, nct, eq, ne)</li>
         * <li><code>vendorCustomField2</code> (ct, nct, eq, ne)</li>
         * <li><code>comments</code> (ct, nct)</li>
         * <li><code>payaccountCurrency</code> (ne, eq, in, nin)</li>
         * <li><code>remitAddress</code> (ct, nct)</li>
         * <li><code>source</code> (ct, nct, eq, ne)</li>
         * <li><code>updatedOn</code> (gt, ge, lt, le, eq, ne)</li>
         * <li><code>feeAmount</code> (gt, ge, lt, le, eq, ne)</li>
         * <li><code>lotNumber</code> (ct, nct)</li>
         * <li><code>customerVendorAccount</code> (ct, nct, eq, ne)</li>
         * <li><code>batchId</code> (eq, ne)</li>
         * <li><code>AchTraceNumber</code> (eq, ne)</li>
         * <li><code>payoutProgram</code>(eq, ne) the options are <code>managed</code> or <code>odp</code>. For example, <code>payoutProgram(eq)=managed</code> returns all records with a <code>payoutProgram</code> equal to <code>managed</code>.</li>
         * </ul>
         * <p>List of comparison accepted - enclosed between parentheses:</p>
         * <ul>
         * <li>eq or empty =&gt; equal</li>
         * <li>gt =&gt; greater than</li>
         * <li>ge =&gt; greater or equal</li>
         * <li>lt =&gt; less than</li>
         * <li>le =&gt; less or equal</li>
         * <li>ne =&gt; not equal</li>
         * <li>ct =&gt; contains</li>
         * <li>nct =&gt; not contains</li>
         * <li>in =&gt; inside array separated by &quot;|&quot;</li>
         * <li>nin =&gt; not inside array separated by &quot;|&quot;</li>
         * </ul>
         * <p>List of parameters accepted:</p>
         * <ul>
         * <li>limitRecord : max number of records for query (default=&quot;20&quot;, &quot;0&quot; or negative value for all)</li>
         * <li>fromRecord : initial record in query</li>
         * <li>sortBy : indicate field name and direction to sort the results</li>
         * </ul>
         * <p>Example: <code>netAmount(gt)=20</code> returns all records with a <code>netAmount</code> greater than 20.00</p>
         * <p>Example: <code>sortBy=desc(netamount)</code> returns all records sorted by <code>netAmount</code> descending</p>
         */
        @JsonSetter(value = "parameters", nulls = Nulls.SKIP)
        public Builder parameters(Optional<Map<String, Optional<String>>> parameters) {
            this.parameters = parameters;
            return this;
        }

        public Builder parameters(Map<String, Optional<String>> parameters) {
            this.parameters = Optional.ofNullable(parameters);
            return this;
        }

        /**
         * <p>The field name to use for sorting results. Use <code>desc(field_name)</code> to sort descending by <code>field_name</code>, and use <code>asc(field_name)</code> to sort ascending by <code>field_name</code>.</p>
         */
        @JsonSetter(value = "sortBy", nulls = Nulls.SKIP)
        public Builder sortBy(Optional<String> sortBy) {
            this.sortBy = sortBy;
            return this;
        }

        public Builder sortBy(String sortBy) {
            this.sortBy = Optional.ofNullable(sortBy);
            return this;
        }

        public ListPayoutOrgRequest build() {
            return new ListPayoutOrgRequest(
                    exportFormat, fromRecord, limitRecord, parameters, sortBy, additionalProperties);
        }
    }
}

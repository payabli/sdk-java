/**
 * This file was auto-generated by Fern from our API Definition.
 */
package io.github.payabli.api.resources.moneyout;

import com.fasterxml.jackson.core.JsonProcessingException;
import io.github.payabli.api.core.ClientOptions;
import io.github.payabli.api.core.EndpointMetadata;
import io.github.payabli.api.core.MediaTypes;
import io.github.payabli.api.core.ObjectMappers;
import io.github.payabli.api.core.PayabliApiClientApiException;
import io.github.payabli.api.core.PayabliApiClientException;
import io.github.payabli.api.core.PayabliApiClientHttpResponse;
import io.github.payabli.api.core.QueryStringMapper;
import io.github.payabli.api.core.RequestOptions;
import io.github.payabli.api.core.RetryInterceptor;
import io.github.payabli.api.errors.BadRequestError;
import io.github.payabli.api.errors.ConflictError;
import io.github.payabli.api.errors.ForbiddenError;
import io.github.payabli.api.errors.InternalServerError;
import io.github.payabli.api.errors.ServiceUnavailableError;
import io.github.payabli.api.errors.UnauthorizedError;
import io.github.payabli.api.errors.UnprocessableEntityError;
import io.github.payabli.api.resources.moneyout.requests.CaptureAllOutRequest;
import io.github.payabli.api.resources.moneyout.requests.CaptureOutRequest;
import io.github.payabli.api.resources.moneyout.requests.PayoutRequest;
import io.github.payabli.api.resources.moneyout.requests.ReissueOutRequest;
import io.github.payabli.api.resources.moneyout.requests.RenewVCardRequest;
import io.github.payabli.api.resources.moneyout.requests.RequestOutAuthorize;
import io.github.payabli.api.resources.moneyout.requests.SendVCardLinkRequest;
import io.github.payabli.api.types.AllowedCheckPaymentStatus;
import io.github.payabli.api.types.AuthCapturePayoutResponse;
import io.github.payabli.api.types.AuthorizePayoutBody;
import io.github.payabli.api.types.BillDetailResponse;
import io.github.payabli.api.types.CaptureAllOutResponse;
import io.github.payabli.api.types.OperationResult;
import io.github.payabli.api.types.PayabliApiResponse0000;
import io.github.payabli.api.types.PayabliApiResponse00Responsedatanonobject;
import io.github.payabli.api.types.PayabliErrorBody;
import io.github.payabli.api.types.ReissuePayoutResponse;
import io.github.payabli.api.types.RenewVCardResponse;
import io.github.payabli.api.types.VCardGetResponse;
import java.io.IOException;
import java.util.HashMap;
import java.util.List;
import java.util.Map;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawMoneyOutClient {
    protected final ClientOptions clientOptions;

    public RawMoneyOutClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Authorizes a transaction for payout.
     * <p>If you don't pass <code>autoCapture</code> with a value of <code>true</code>, authorized transactions aren't flagged for settlement until captured. Use the <code>referenceId</code> returned in the response to capture the transaction.</p>
     * <p>When <code>autoCapture</code> is <code>true</code>, Payabli captures the transaction asynchronously after authorization. The response confirms only that the transaction was authorized; it doesn't confirm that capture succeeded. To confirm capture, listen for the <a href="/developers/webhooks/payout-transaction-approved-captured"><code>payout_transaction_approvedcaptured</code></a> webhook event.</p>
     * <p>If a velocity fraud alert is triggered, the endpoint returns a <code>202</code> response with <code>responseCode</code> <code>9051</code>, and the authorization is held for risk review rather than rejected. If a risk policy blocks the transaction, the endpoint returns a <code>422</code> response with <code>responseCode</code> <code>9005</code>, a terminal rejection.</p>
     * <p>For check payouts, Payabli validates the remit (mailing) address at authorization. If the address fails deliverability validation, the endpoint returns a <code>422</code> response and doesn't charge the paypoint. Correct the address and re-authorize. Other payout rails (ACH, RTP, virtual card, wire, and managed payables) aren't affected.</p>
     */
    public PayabliApiClientHttpResponse<AuthCapturePayoutResponse> authorizeOut(AuthorizePayoutBody body) {
        return authorizeOut(RequestOutAuthorize.builder().body(body).build());
    }

    /**
     * Authorizes a transaction for payout.
     * <p>If you don't pass <code>autoCapture</code> with a value of <code>true</code>, authorized transactions aren't flagged for settlement until captured. Use the <code>referenceId</code> returned in the response to capture the transaction.</p>
     * <p>When <code>autoCapture</code> is <code>true</code>, Payabli captures the transaction asynchronously after authorization. The response confirms only that the transaction was authorized; it doesn't confirm that capture succeeded. To confirm capture, listen for the <a href="/developers/webhooks/payout-transaction-approved-captured"><code>payout_transaction_approvedcaptured</code></a> webhook event.</p>
     * <p>If a velocity fraud alert is triggered, the endpoint returns a <code>202</code> response with <code>responseCode</code> <code>9051</code>, and the authorization is held for risk review rather than rejected. If a risk policy blocks the transaction, the endpoint returns a <code>422</code> response with <code>responseCode</code> <code>9005</code>, a terminal rejection.</p>
     * <p>For check payouts, Payabli validates the remit (mailing) address at authorization. If the address fails deliverability validation, the endpoint returns a <code>422</code> response and doesn't charge the paypoint. Correct the address and re-authorize. Other payout rails (ACH, RTP, virtual card, wire, and managed payables) aren't affected.</p>
     */
    public PayabliApiClientHttpResponse<AuthCapturePayoutResponse> authorizeOut(
            AuthorizePayoutBody body, RequestOptions requestOptions) {
        return authorizeOut(RequestOutAuthorize.builder().body(body).build(), requestOptions);
    }

    /**
     * Authorizes a transaction for payout.
     * <p>If you don't pass <code>autoCapture</code> with a value of <code>true</code>, authorized transactions aren't flagged for settlement until captured. Use the <code>referenceId</code> returned in the response to capture the transaction.</p>
     * <p>When <code>autoCapture</code> is <code>true</code>, Payabli captures the transaction asynchronously after authorization. The response confirms only that the transaction was authorized; it doesn't confirm that capture succeeded. To confirm capture, listen for the <a href="/developers/webhooks/payout-transaction-approved-captured"><code>payout_transaction_approvedcaptured</code></a> webhook event.</p>
     * <p>If a velocity fraud alert is triggered, the endpoint returns a <code>202</code> response with <code>responseCode</code> <code>9051</code>, and the authorization is held for risk review rather than rejected. If a risk policy blocks the transaction, the endpoint returns a <code>422</code> response with <code>responseCode</code> <code>9005</code>, a terminal rejection.</p>
     * <p>For check payouts, Payabli validates the remit (mailing) address at authorization. If the address fails deliverability validation, the endpoint returns a <code>422</code> response and doesn't charge the paypoint. Correct the address and re-authorize. Other payout rails (ACH, RTP, virtual card, wire, and managed payables) aren't affected.</p>
     */
    public PayabliApiClientHttpResponse<AuthCapturePayoutResponse> authorizeOut(RequestOutAuthorize request) {
        return authorizeOut(request, null);
    }

    /**
     * Authorizes a transaction for payout.
     * <p>If you don't pass <code>autoCapture</code> with a value of <code>true</code>, authorized transactions aren't flagged for settlement until captured. Use the <code>referenceId</code> returned in the response to capture the transaction.</p>
     * <p>When <code>autoCapture</code> is <code>true</code>, Payabli captures the transaction asynchronously after authorization. The response confirms only that the transaction was authorized; it doesn't confirm that capture succeeded. To confirm capture, listen for the <a href="/developers/webhooks/payout-transaction-approved-captured"><code>payout_transaction_approvedcaptured</code></a> webhook event.</p>
     * <p>If a velocity fraud alert is triggered, the endpoint returns a <code>202</code> response with <code>responseCode</code> <code>9051</code>, and the authorization is held for risk review rather than rejected. If a risk policy blocks the transaction, the endpoint returns a <code>422</code> response with <code>responseCode</code> <code>9005</code>, a terminal rejection.</p>
     * <p>For check payouts, Payabli validates the remit (mailing) address at authorization. If the address fails deliverability validation, the endpoint returns a <code>422</code> response and doesn't charge the paypoint. Correct the address and re-authorize. Other payout rails (ACH, RTP, virtual card, wire, and managed payables) aren't affected.</p>
     */
    public PayabliApiClientHttpResponse<AuthCapturePayoutResponse> authorizeOut(
            RequestOutAuthorize request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("MoneyOut/authorize");
        if (request.getAllowDuplicatedBills().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl,
                    "allowDuplicatedBills",
                    request.getAllowDuplicatedBills().get(),
                    false);
        }
        if (request.getDoNotCreateBills().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "doNotCreateBills", request.getDoNotCreateBills().get(), false);
        }
        if (request.getSameDayAch().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "sameDayACH", request.getSameDayAch().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Map<String, String> _headers = new HashMap<>(clientOptions.headers(requestOptions));
        _headers.putAll(clientOptions.getAuthHeaders(EndpointMetadata.of(
                EndpointMetadata.requirement(EndpointMetadata.scheme("BearerAuth")),
                EndpointMetadata.requirement(EndpointMetadata.scheme("APIKeyAuth")))));
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(_headers))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        if (request.getIdempotencyKey().isPresent()) {
            _requestBuilder.addHeader(
                    "idempotencyKey", request.getIdempotencyKey().get());
        }
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PayabliApiClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, AuthCapturePayoutResponse.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PayabliErrorBody.class),
                                response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PayabliErrorBody.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 503:
                        throw new ServiceUnavailableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PayabliErrorBody.class),
                                response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayabliApiClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new PayabliApiClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new PayabliApiClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Cancels an array of payout transactions.
     */
    public PayabliApiClientHttpResponse<CaptureAllOutResponse> cancelAllOut(List<String> request) {
        return cancelAllOut(request, null);
    }

    /**
     * Cancels an array of payout transactions.
     */
    public PayabliApiClientHttpResponse<CaptureAllOutResponse> cancelAllOut(
            List<String> request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("MoneyOut/cancelAll");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new PayabliApiClientException("Failed to serialize request", e);
        }
        Map<String, String> _headers = new HashMap<>(clientOptions.headers(requestOptions));
        _headers.putAll(clientOptions.getAuthHeaders(EndpointMetadata.of(
                EndpointMetadata.requirement(EndpointMetadata.scheme("BearerAuth")),
                EndpointMetadata.requirement(EndpointMetadata.scheme("APIKeyAuth")))));
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(_headers))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PayabliApiClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CaptureAllOutResponse.class), response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PayabliErrorBody.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 503:
                        throw new ServiceUnavailableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PayabliErrorBody.class),
                                response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayabliApiClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new PayabliApiClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new PayabliApiClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Cancel a payout transaction by ID.
     */
    public PayabliApiClientHttpResponse<PayabliApiResponse0000> cancelOutGet(String referenceId) {
        return cancelOutGet(referenceId, null);
    }

    /**
     * Cancel a payout transaction by ID.
     */
    public PayabliApiClientHttpResponse<PayabliApiResponse0000> cancelOutGet(
            String referenceId, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("MoneyOut/cancel")
                .addPathSegment(referenceId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Map<String, String> _headers = new HashMap<>(clientOptions.headers(requestOptions));
        _headers.putAll(clientOptions.getAuthHeaders(EndpointMetadata.of(
                EndpointMetadata.requirement(EndpointMetadata.scheme("BearerAuth")),
                EndpointMetadata.requirement(EndpointMetadata.scheme("APIKeyAuth")))));
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(_headers))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PayabliApiClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PayabliApiResponse0000.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PayabliErrorBody.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 503:
                        throw new ServiceUnavailableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PayabliErrorBody.class),
                                response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayabliApiClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new PayabliApiClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new PayabliApiClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Cancel a payout transaction by ID.
     */
    public PayabliApiClientHttpResponse<PayabliApiResponse0000> cancelOutDelete(String referenceId) {
        return cancelOutDelete(referenceId, null);
    }

    /**
     * Cancel a payout transaction by ID.
     */
    public PayabliApiClientHttpResponse<PayabliApiResponse0000> cancelOutDelete(
            String referenceId, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("MoneyOut/cancel")
                .addPathSegment(referenceId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Map<String, String> _headers = new HashMap<>(clientOptions.headers(requestOptions));
        _headers.putAll(clientOptions.getAuthHeaders(EndpointMetadata.of(
                EndpointMetadata.requirement(EndpointMetadata.scheme("BearerAuth")),
                EndpointMetadata.requirement(EndpointMetadata.scheme("APIKeyAuth")))));
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(_headers))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PayabliApiClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PayabliApiResponse0000.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PayabliErrorBody.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 503:
                        throw new ServiceUnavailableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PayabliErrorBody.class),
                                response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayabliApiClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new PayabliApiClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new PayabliApiClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Captures an array of authorized payout transactions for settlement. The maximum number of transactions that can be captured in a single request is 500.
     */
    public PayabliApiClientHttpResponse<CaptureAllOutResponse> captureAllOut(List<String> body) {
        return captureAllOut(CaptureAllOutRequest.builder().body(body).build());
    }

    /**
     * Captures an array of authorized payout transactions for settlement. The maximum number of transactions that can be captured in a single request is 500.
     */
    public PayabliApiClientHttpResponse<CaptureAllOutResponse> captureAllOut(
            List<String> body, RequestOptions requestOptions) {
        return captureAllOut(CaptureAllOutRequest.builder().body(body).build(), requestOptions);
    }

    /**
     * Captures an array of authorized payout transactions for settlement. The maximum number of transactions that can be captured in a single request is 500.
     */
    public PayabliApiClientHttpResponse<CaptureAllOutResponse> captureAllOut(CaptureAllOutRequest request) {
        return captureAllOut(request, null);
    }

    /**
     * Captures an array of authorized payout transactions for settlement. The maximum number of transactions that can be captured in a single request is 500.
     */
    public PayabliApiClientHttpResponse<CaptureAllOutResponse> captureAllOut(
            CaptureAllOutRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("MoneyOut/captureAll");
        if (request.getAutoConvertSameDayAch().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl,
                    "autoConvertSameDayAch",
                    request.getAutoConvertSameDayAch().get(),
                    false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Map<String, String> _headers = new HashMap<>(clientOptions.headers(requestOptions));
        _headers.putAll(clientOptions.getAuthHeaders(EndpointMetadata.of(
                EndpointMetadata.requirement(EndpointMetadata.scheme("BearerAuth")),
                EndpointMetadata.requirement(EndpointMetadata.scheme("APIKeyAuth")))));
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(_headers))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        if (request.getIdempotencyKey().isPresent()) {
            _requestBuilder.addHeader(
                    "idempotencyKey", request.getIdempotencyKey().get());
        }
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PayabliApiClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CaptureAllOutResponse.class), response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PayabliErrorBody.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 503:
                        throw new ServiceUnavailableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PayabliErrorBody.class),
                                response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayabliApiClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new PayabliApiClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new PayabliApiClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Captures a single authorized payout transaction by ID. If the transaction was authorized with <code>autoCapture</code> set to <code>true</code>, you don't need to call this endpoint to capture the transaction for processing.
     * <p>If a velocity fraud alert is triggered, the endpoint returns a <code>202</code> response with <code>responseCode</code> <code>9051</code>, and the capture is held for risk review rather than rejected. If a risk policy blocks the transaction, the endpoint returns a <code>422</code> response with <code>responseCode</code> <code>9005</code>, a terminal rejection.</p>
     */
    public PayabliApiClientHttpResponse<AuthCapturePayoutResponse> captureOut(String referenceId) {
        return captureOut(referenceId, CaptureOutRequest.builder().build());
    }

    /**
     * Captures a single authorized payout transaction by ID. If the transaction was authorized with <code>autoCapture</code> set to <code>true</code>, you don't need to call this endpoint to capture the transaction for processing.
     * <p>If a velocity fraud alert is triggered, the endpoint returns a <code>202</code> response with <code>responseCode</code> <code>9051</code>, and the capture is held for risk review rather than rejected. If a risk policy blocks the transaction, the endpoint returns a <code>422</code> response with <code>responseCode</code> <code>9005</code>, a terminal rejection.</p>
     */
    public PayabliApiClientHttpResponse<AuthCapturePayoutResponse> captureOut(
            String referenceId, RequestOptions requestOptions) {
        return captureOut(referenceId, CaptureOutRequest.builder().build(), requestOptions);
    }

    /**
     * Captures a single authorized payout transaction by ID. If the transaction was authorized with <code>autoCapture</code> set to <code>true</code>, you don't need to call this endpoint to capture the transaction for processing.
     * <p>If a velocity fraud alert is triggered, the endpoint returns a <code>202</code> response with <code>responseCode</code> <code>9051</code>, and the capture is held for risk review rather than rejected. If a risk policy blocks the transaction, the endpoint returns a <code>422</code> response with <code>responseCode</code> <code>9005</code>, a terminal rejection.</p>
     */
    public PayabliApiClientHttpResponse<AuthCapturePayoutResponse> captureOut(
            String referenceId, CaptureOutRequest request) {
        return captureOut(referenceId, request, null);
    }

    /**
     * Captures a single authorized payout transaction by ID. If the transaction was authorized with <code>autoCapture</code> set to <code>true</code>, you don't need to call this endpoint to capture the transaction for processing.
     * <p>If a velocity fraud alert is triggered, the endpoint returns a <code>202</code> response with <code>responseCode</code> <code>9051</code>, and the capture is held for risk review rather than rejected. If a risk policy blocks the transaction, the endpoint returns a <code>422</code> response with <code>responseCode</code> <code>9005</code>, a terminal rejection.</p>
     */
    public PayabliApiClientHttpResponse<AuthCapturePayoutResponse> captureOut(
            String referenceId, CaptureOutRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("MoneyOut/capture")
                .addPathSegment(referenceId);
        if (request.getAutoConvertSameDayAch().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl,
                    "autoConvertSameDayAch",
                    request.getAutoConvertSameDayAch().get(),
                    false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Map<String, String> _headers = new HashMap<>(clientOptions.headers(requestOptions));
        _headers.putAll(clientOptions.getAuthHeaders(EndpointMetadata.of(
                EndpointMetadata.requirement(EndpointMetadata.scheme("BearerAuth")),
                EndpointMetadata.requirement(EndpointMetadata.scheme("APIKeyAuth")))));
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(_headers))
                .addHeader("Accept", "application/json");
        if (request.getIdempotencyKey().isPresent()) {
            _requestBuilder.addHeader(
                    "idempotencyKey", request.getIdempotencyKey().get());
        }
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PayabliApiClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, AuthCapturePayoutResponse.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PayabliErrorBody.class),
                                response);
                    case 409:
                        throw new ConflictError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PayabliErrorBody.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 503:
                        throw new ServiceUnavailableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PayabliErrorBody.class),
                                response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayabliApiClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new PayabliApiClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new PayabliApiClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Authorizes a payout and captures it in the same request, returning the capture result. Use this endpoint when you need the capture outcome synchronously: it does the same work as calling <code>POST /MoneyOut/authorize</code> followed by <code>GET /MoneyOut/capture/{referenceId}</code>, in a single call.
     * <p>Risk and fraud review runs at both the authorize and capture stages, exactly as it does for the two-call flow.</p>
     * <p>Payabli ignores the <code>autoCapture</code> field in the request body, since this endpoint always captures inline.</p>
     * <p>If the capture fails, the payout stays authorized. Retry the capture with <code>GET /MoneyOut/capture/{referenceId}</code> using the <code>referenceId</code> from the error response rather than resubmitting, which would create a second payout. See the <a href="/guides/pay-out-developer-payouts-manage#authorize-and-capture-in-one-call">Manage payouts guide</a> for details.</p>
     */
    public PayabliApiClientHttpResponse<AuthCapturePayoutResponse> payout(AuthorizePayoutBody body) {
        return payout(PayoutRequest.builder().body(body).build());
    }

    /**
     * Authorizes a payout and captures it in the same request, returning the capture result. Use this endpoint when you need the capture outcome synchronously: it does the same work as calling <code>POST /MoneyOut/authorize</code> followed by <code>GET /MoneyOut/capture/{referenceId}</code>, in a single call.
     * <p>Risk and fraud review runs at both the authorize and capture stages, exactly as it does for the two-call flow.</p>
     * <p>Payabli ignores the <code>autoCapture</code> field in the request body, since this endpoint always captures inline.</p>
     * <p>If the capture fails, the payout stays authorized. Retry the capture with <code>GET /MoneyOut/capture/{referenceId}</code> using the <code>referenceId</code> from the error response rather than resubmitting, which would create a second payout. See the <a href="/guides/pay-out-developer-payouts-manage#authorize-and-capture-in-one-call">Manage payouts guide</a> for details.</p>
     */
    public PayabliApiClientHttpResponse<AuthCapturePayoutResponse> payout(
            AuthorizePayoutBody body, RequestOptions requestOptions) {
        return payout(PayoutRequest.builder().body(body).build(), requestOptions);
    }

    /**
     * Authorizes a payout and captures it in the same request, returning the capture result. Use this endpoint when you need the capture outcome synchronously: it does the same work as calling <code>POST /MoneyOut/authorize</code> followed by <code>GET /MoneyOut/capture/{referenceId}</code>, in a single call.
     * <p>Risk and fraud review runs at both the authorize and capture stages, exactly as it does for the two-call flow.</p>
     * <p>Payabli ignores the <code>autoCapture</code> field in the request body, since this endpoint always captures inline.</p>
     * <p>If the capture fails, the payout stays authorized. Retry the capture with <code>GET /MoneyOut/capture/{referenceId}</code> using the <code>referenceId</code> from the error response rather than resubmitting, which would create a second payout. See the <a href="/guides/pay-out-developer-payouts-manage#authorize-and-capture-in-one-call">Manage payouts guide</a> for details.</p>
     */
    public PayabliApiClientHttpResponse<AuthCapturePayoutResponse> payout(PayoutRequest request) {
        return payout(request, null);
    }

    /**
     * Authorizes a payout and captures it in the same request, returning the capture result. Use this endpoint when you need the capture outcome synchronously: it does the same work as calling <code>POST /MoneyOut/authorize</code> followed by <code>GET /MoneyOut/capture/{referenceId}</code>, in a single call.
     * <p>Risk and fraud review runs at both the authorize and capture stages, exactly as it does for the two-call flow.</p>
     * <p>Payabli ignores the <code>autoCapture</code> field in the request body, since this endpoint always captures inline.</p>
     * <p>If the capture fails, the payout stays authorized. Retry the capture with <code>GET /MoneyOut/capture/{referenceId}</code> using the <code>referenceId</code> from the error response rather than resubmitting, which would create a second payout. See the <a href="/guides/pay-out-developer-payouts-manage#authorize-and-capture-in-one-call">Manage payouts guide</a> for details.</p>
     */
    public PayabliApiClientHttpResponse<AuthCapturePayoutResponse> payout(
            PayoutRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("MoneyOut/payout");
        if (request.getSameDayAch().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "sameDayACH", request.getSameDayAch().get(), false);
        }
        if (request.getDoNotCreateBills().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "doNotCreateBills", request.getDoNotCreateBills().get(), false);
        }
        if (request.getAllowDuplicatedBills().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl,
                    "allowDuplicatedBills",
                    request.getAllowDuplicatedBills().get(),
                    false);
        }
        if (request.getUpdateVendorPaymentMethod().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl,
                    "updateVendorPaymentMethod",
                    request.getUpdateVendorPaymentMethod().get(),
                    false);
        }
        if (request.getAutoConvertSameDayAch().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl,
                    "autoConvertSameDayAch",
                    request.getAutoConvertSameDayAch().get(),
                    false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Map<String, String> _headers = new HashMap<>(clientOptions.headers(requestOptions));
        _headers.putAll(clientOptions.getAuthHeaders(EndpointMetadata.of(
                EndpointMetadata.requirement(EndpointMetadata.scheme("BearerAuth")),
                EndpointMetadata.requirement(EndpointMetadata.scheme("APIKeyAuth")))));
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(_headers))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        if (request.getIdempotencyKey().isPresent()) {
            _requestBuilder.addHeader(
                    "idempotencyKey", request.getIdempotencyKey().get());
        }
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PayabliApiClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, AuthCapturePayoutResponse.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PayabliErrorBody.class),
                                response);
                    case 409:
                        throw new ConflictError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 422:
                        throw new UnprocessableEntityError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PayabliErrorBody.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 503:
                        throw new ServiceUnavailableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PayabliErrorBody.class),
                                response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayabliApiClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new PayabliApiClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new PayabliApiClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Returns details for a processed money out transaction.
     */
    public PayabliApiClientHttpResponse<BillDetailResponse> payoutDetails(String transId) {
        return payoutDetails(transId, null);
    }

    /**
     * Returns details for a processed money out transaction.
     */
    public PayabliApiClientHttpResponse<BillDetailResponse> payoutDetails(
            String transId, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("MoneyOut/details")
                .addPathSegment(transId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Map<String, String> _headers = new HashMap<>(clientOptions.headers(requestOptions));
        _headers.putAll(clientOptions.getAuthHeaders(EndpointMetadata.of(
                EndpointMetadata.requirement(EndpointMetadata.scheme("BearerAuth")),
                EndpointMetadata.requirement(EndpointMetadata.scheme("APIKeyAuth")))));
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(_headers))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PayabliApiClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, BillDetailResponse.class), response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PayabliErrorBody.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 503:
                        throw new ServiceUnavailableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PayabliErrorBody.class),
                                response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayabliApiClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new PayabliApiClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new PayabliApiClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Retrieves vCard details for a single card in an entrypoint.
     */
    public PayabliApiClientHttpResponse<VCardGetResponse> vCardGet(String cardToken) {
        return vCardGet(cardToken, null);
    }

    /**
     * Retrieves vCard details for a single card in an entrypoint.
     */
    public PayabliApiClientHttpResponse<VCardGetResponse> vCardGet(String cardToken, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("MoneyOut/vcard")
                .addPathSegment(cardToken);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Map<String, String> _headers = new HashMap<>(clientOptions.headers(requestOptions));
        _headers.putAll(clientOptions.getAuthHeaders(EndpointMetadata.of(
                EndpointMetadata.requirement(EndpointMetadata.scheme("BearerAuth")),
                EndpointMetadata.requirement(EndpointMetadata.scheme("APIKeyAuth")))));
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(_headers))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PayabliApiClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, VCardGetResponse.class), response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PayabliErrorBody.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 503:
                        throw new ServiceUnavailableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PayabliErrorBody.class),
                                response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayabliApiClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new PayabliApiClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new PayabliApiClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Renews an expired or expiring virtual card by extending its expiration date to a future month.
     * <p>The card must be a virtual card that hasn't been fully used. The new expiration date must be in <code>MM-YYYY</code> or <code>MM/YYYY</code> format and no more than 2 years and 363 days in the future. The card expires on the last day of the month you specify.</p>
     * <p>On success, <code>referenceId</code> holds the renewed card's token (the card processor may issue a new token). The response reuses the standard payout result object, so the payment-transaction fields it carries don't apply to renewal and always return <code>null</code>.</p>
     */
    public PayabliApiClientHttpResponse<RenewVCardResponse> renewVCard(String cardToken, RenewVCardRequest request) {
        return renewVCard(cardToken, request, null);
    }

    /**
     * Renews an expired or expiring virtual card by extending its expiration date to a future month.
     * <p>The card must be a virtual card that hasn't been fully used. The new expiration date must be in <code>MM-YYYY</code> or <code>MM/YYYY</code> format and no more than 2 years and 363 days in the future. The card expires on the last day of the month you specify.</p>
     * <p>On success, <code>referenceId</code> holds the renewed card's token (the card processor may issue a new token). The response reuses the standard payout result object, so the payment-transaction fields it carries don't apply to renewal and always return <code>null</code>.</p>
     */
    public PayabliApiClientHttpResponse<RenewVCardResponse> renewVCard(
            String cardToken, RenewVCardRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("MoneyOutCard/vcard")
                .addPathSegment(cardToken)
                .addPathSegments("renew");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new PayabliApiClientException("Failed to serialize request", e);
        }
        Map<String, String> _headers = new HashMap<>(clientOptions.headers(requestOptions));
        _headers.putAll(clientOptions.getAuthHeaders(EndpointMetadata.of(
                EndpointMetadata.requirement(EndpointMetadata.scheme("BearerAuth")),
                EndpointMetadata.requirement(EndpointMetadata.scheme("APIKeyAuth")))));
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("PUT", body)
                .headers(Headers.of(_headers))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PayabliApiClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, RenewVCardResponse.class), response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PayabliErrorBody.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 503:
                        throw new ServiceUnavailableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PayabliErrorBody.class),
                                response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayabliApiClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new PayabliApiClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new PayabliApiClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Sends a virtual card link via email to the vendor associated with the <code>transId</code>.
     */
    public PayabliApiClientHttpResponse<OperationResult> sendVCardLink(SendVCardLinkRequest request) {
        return sendVCardLink(request, null);
    }

    /**
     * Sends a virtual card link via email to the vendor associated with the <code>transId</code>.
     */
    public PayabliApiClientHttpResponse<OperationResult> sendVCardLink(
            SendVCardLinkRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("MoneyOut/vcard/send-card-link");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new PayabliApiClientException("Failed to serialize request", e);
        }
        Map<String, String> _headers = new HashMap<>(clientOptions.headers(requestOptions));
        _headers.putAll(clientOptions.getAuthHeaders(EndpointMetadata.of(
                EndpointMetadata.requirement(EndpointMetadata.scheme("BearerAuth")),
                EndpointMetadata.requirement(EndpointMetadata.scheme("APIKeyAuth")))));
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(_headers))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PayabliApiClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, OperationResult.class), response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PayabliErrorBody.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 503:
                        throw new ServiceUnavailableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PayabliErrorBody.class),
                                response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayabliApiClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new PayabliApiClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new PayabliApiClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Retrieve the image of a check associated with a processed transaction.
     * The check image is returned in the response body as a base64-encoded string.
     * The check image is only available for payouts that have been processed.
     */
    public PayabliApiClientHttpResponse<String> getCheckImage(String assetName) {
        return getCheckImage(assetName, null);
    }

    /**
     * Retrieve the image of a check associated with a processed transaction.
     * The check image is returned in the response body as a base64-encoded string.
     * The check image is only available for payouts that have been processed.
     */
    public PayabliApiClientHttpResponse<String> getCheckImage(String assetName, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("MoneyOut/checkimage")
                .addPathSegment(assetName);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Map<String, String> _headers = new HashMap<>(clientOptions.headers(requestOptions));
        _headers.putAll(clientOptions.getAuthHeaders(EndpointMetadata.of(
                EndpointMetadata.requirement(EndpointMetadata.scheme("BearerAuth")),
                EndpointMetadata.requirement(EndpointMetadata.scheme("APIKeyAuth")))));
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(_headers))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PayabliApiClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, String.class), response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PayabliErrorBody.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 503:
                        throw new ServiceUnavailableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PayabliErrorBody.class),
                                response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayabliApiClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new PayabliApiClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new PayabliApiClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Updates the status of a processed check payment transaction. This endpoint handles the status transition, updates related bills, creates audit events, and triggers notifications.
     * <p>The transaction must meet all of the following criteria:</p>
     * <ul>
     * <li><strong>Status</strong>: Must be in Processing or Processed status.</li>
     * <li><strong>Payment method</strong>: Must be a check payment method.</li>
     * </ul>
     * <h3>Allowed status values</h3>
     * <p>| Value | Status | Description |
     * |-------|--------|-------------|
     * | <code>0</code> | Cancelled/Voided | Cancels the check transaction. Reverts associated bills to their previous state (Approved or Active), creates &quot;Cancelled&quot; events, and sends a <code>payout_transaction_voidedcancelled</code> notification if the notification is enabled. |
     * | <code>5</code> | Paid | Marks the check transaction as paid. Updates associated bills to &quot;Paid&quot; status, creates &quot;Paid&quot; events, and sends a <code>payout_transaction_paid</code> notification if the notification is enabled. |</p>
     */
    public PayabliApiClientHttpResponse<PayabliApiResponse00Responsedatanonobject> updateCheckPaymentStatus(
            String transId, AllowedCheckPaymentStatus checkPaymentStatus) {
        return updateCheckPaymentStatus(transId, checkPaymentStatus, null);
    }

    /**
     * Updates the status of a processed check payment transaction. This endpoint handles the status transition, updates related bills, creates audit events, and triggers notifications.
     * <p>The transaction must meet all of the following criteria:</p>
     * <ul>
     * <li><strong>Status</strong>: Must be in Processing or Processed status.</li>
     * <li><strong>Payment method</strong>: Must be a check payment method.</li>
     * </ul>
     * <h3>Allowed status values</h3>
     * <p>| Value | Status | Description |
     * |-------|--------|-------------|
     * | <code>0</code> | Cancelled/Voided | Cancels the check transaction. Reverts associated bills to their previous state (Approved or Active), creates &quot;Cancelled&quot; events, and sends a <code>payout_transaction_voidedcancelled</code> notification if the notification is enabled. |
     * | <code>5</code> | Paid | Marks the check transaction as paid. Updates associated bills to &quot;Paid&quot; status, creates &quot;Paid&quot; events, and sends a <code>payout_transaction_paid</code> notification if the notification is enabled. |</p>
     */
    public PayabliApiClientHttpResponse<PayabliApiResponse00Responsedatanonobject> updateCheckPaymentStatus(
            String transId, AllowedCheckPaymentStatus checkPaymentStatus, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("MoneyOut/status")
                .addPathSegment(transId)
                .addPathSegment(checkPaymentStatus.toString());
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Map<String, String> _headers = new HashMap<>(clientOptions.headers(requestOptions));
        _headers.putAll(clientOptions.getAuthHeaders(EndpointMetadata.of(
                EndpointMetadata.requirement(EndpointMetadata.scheme("BearerAuth")),
                EndpointMetadata.requirement(EndpointMetadata.scheme("APIKeyAuth")))));
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("PATCH", RequestBody.create("", null))
                .headers(Headers.of(_headers))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PayabliApiClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString, PayabliApiResponse00Responsedatanonobject.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PayabliErrorBody.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 503:
                        throw new ServiceUnavailableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PayabliErrorBody.class),
                                response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayabliApiClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new PayabliApiClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new PayabliApiClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * Reissues a payout transaction with a new payment method. This creates a new transaction linked to the original and marks the original transaction as reissued.
     * <p>The original transaction must be in <strong>Processing</strong> or <strong>Processed</strong> status. The payment method in the request body is used directly. The endpoint doesn't fall back to vendor-managed payment methods.</p>
     * <p>The new transaction goes through the standard authorize-and-capture flow automatically. Both the original and new transactions are linked through their event histories for audit purposes.</p>
     */
    public PayabliApiClientHttpResponse<ReissuePayoutResponse> reissueOut(ReissueOutRequest request) {
        return reissueOut(request, null);
    }

    /**
     * Reissues a payout transaction with a new payment method. This creates a new transaction linked to the original and marks the original transaction as reissued.
     * <p>The original transaction must be in <strong>Processing</strong> or <strong>Processed</strong> status. The payment method in the request body is used directly. The endpoint doesn't fall back to vendor-managed payment methods.</p>
     * <p>The new transaction goes through the standard authorize-and-capture flow automatically. Both the original and new transactions are linked through their event histories for audit purposes.</p>
     */
    public PayabliApiClientHttpResponse<ReissuePayoutResponse> reissueOut(
            ReissueOutRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("MoneyOut/reissue");
        QueryStringMapper.addQueryParameter(httpUrl, "transId", request.getTransId(), false);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Map<String, String> _headers = new HashMap<>(clientOptions.headers(requestOptions));
        _headers.putAll(clientOptions.getAuthHeaders(EndpointMetadata.of(
                EndpointMetadata.requirement(EndpointMetadata.scheme("BearerAuth")),
                EndpointMetadata.requirement(EndpointMetadata.scheme("APIKeyAuth")))));
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(_headers))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        if (request.getIdempotencyKey().isPresent()) {
            _requestBuilder.addHeader(
                    "idempotencyKey", request.getIdempotencyKey().get());
        }
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new PayabliApiClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ReissuePayoutResponse.class), response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PayabliErrorBody.class),
                                response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PayabliErrorBody.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 503:
                        throw new ServiceUnavailableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PayabliErrorBody.class),
                                response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new PayabliApiClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new PayabliApiClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new PayabliApiClientException("Network error executing HTTP request", e);
        }
    }
}

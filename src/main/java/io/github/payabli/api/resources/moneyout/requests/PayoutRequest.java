/**
 * This file was auto-generated by Fern from our API Definition.
 */
package io.github.payabli.api.resources.moneyout.requests;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import io.github.payabli.api.core.ObjectMappers;
import io.github.payabli.api.types.AuthorizePayoutBody;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = PayoutRequest.Builder.class)
public final class PayoutRequest {
    private final Optional<String> idempotencyKey;

    private final Optional<Boolean> sameDayAch;

    private final Optional<Boolean> doNotCreateBills;

    private final Optional<Boolean> allowDuplicatedBills;

    private final Optional<Boolean> updateVendorPaymentMethod;

    private final Optional<Boolean> autoConvertSameDayAch;

    private final AuthorizePayoutBody body;

    private final Map<String, Object> additionalProperties;

    private PayoutRequest(
            Optional<String> idempotencyKey,
            Optional<Boolean> sameDayAch,
            Optional<Boolean> doNotCreateBills,
            Optional<Boolean> allowDuplicatedBills,
            Optional<Boolean> updateVendorPaymentMethod,
            Optional<Boolean> autoConvertSameDayAch,
            AuthorizePayoutBody body,
            Map<String, Object> additionalProperties) {
        this.idempotencyKey = idempotencyKey;
        this.sameDayAch = sameDayAch;
        this.doNotCreateBills = doNotCreateBills;
        this.allowDuplicatedBills = allowDuplicatedBills;
        this.updateVendorPaymentMethod = updateVendorPaymentMethod;
        this.autoConvertSameDayAch = autoConvertSameDayAch;
        this.body = body;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return <em>Optional but recommended</em> A unique ID that you can include to prevent duplicating objects or transactions in the case that a request is sent more than once. This key isn't generated in Payabli, you must generate it yourself. This key persists for 2 minutes. After 2 minutes, you can reuse the key if needed.
     */
    @JsonIgnore
    public Optional<String> getIdempotencyKey() {
        return idempotencyKey;
    }

    /**
     * @return When <code>true</code>, Payabli authorizes the payout for same-day ACH processing instead of standard ACH. Same-day ACH must be enabled for the paypoint, otherwise the authorization fails with a <code>400</code> response and <code>responseCode</code> <code>3492</code>. Only ACH payouts honor this flag. Wire and RTP payouts ignore it.
     * <p>Because this endpoint captures immediately, pass <code>autoConvertSameDayAch</code> with a value of <code>true</code> to fall back to standard ACH if the capture runs after the same-day ACH cutoff.</p>
     */
    @JsonProperty("sameDayACH")
    public Optional<Boolean> getSameDayAch() {
        return sameDayAch;
    }

    /**
     * @return When <code>true</code>, Payabli won't automatically create a bill for this payout transaction.
     */
    @JsonProperty("doNotCreateBills")
    public Optional<Boolean> getDoNotCreateBills() {
        return doNotCreateBills;
    }

    /**
     * @return When <code>true</code>, the payout bypasses the requirement for unique bills, identified by vendor invoice number. This allows you to make more than one payout for a bill, like a split payment.
     */
    @JsonProperty("allowDuplicatedBills")
    public Optional<Boolean> getAllowDuplicatedBills() {
        return allowDuplicatedBills;
    }

    /**
     * @return When <code>true</code>, Payabli updates the vendor's stored default payment method to the method used in this payout.
     */
    @JsonProperty("updateVendorPaymentMethod")
    public Optional<Boolean> getUpdateVendorPaymentMethod() {
        return updateVendorPaymentMethod;
    }

    /**
     * @return Controls what happens to a payout authorized with <code>sameDayACH</code> set to <code>true</code> when the capture runs after the same-day ACH cutoff. When <code>true</code>, Payabli converts the payout to a standard ACH payment and captures it. When <code>false</code>, the capture is declined.
     * <p>This parameter has no effect on payouts that weren't authorized for same-day ACH.</p>
     */
    @JsonProperty("autoConvertSameDayAch")
    public Optional<Boolean> getAutoConvertSameDayAch() {
        return autoConvertSameDayAch;
    }

    @JsonProperty("body")
    public AuthorizePayoutBody getBody() {
        return body;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof PayoutRequest && equalTo((PayoutRequest) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(PayoutRequest other) {
        return idempotencyKey.equals(other.idempotencyKey)
                && sameDayAch.equals(other.sameDayAch)
                && doNotCreateBills.equals(other.doNotCreateBills)
                && allowDuplicatedBills.equals(other.allowDuplicatedBills)
                && updateVendorPaymentMethod.equals(other.updateVendorPaymentMethod)
                && autoConvertSameDayAch.equals(other.autoConvertSameDayAch)
                && body.equals(other.body);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.idempotencyKey,
                this.sameDayAch,
                this.doNotCreateBills,
                this.allowDuplicatedBills,
                this.updateVendorPaymentMethod,
                this.autoConvertSameDayAch,
                this.body);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static BodyStage builder() {
        return new Builder();
    }

    public interface BodyStage {
        _FinalStage body(@NotNull AuthorizePayoutBody body);

        Builder from(PayoutRequest other);
    }

    public interface _FinalStage {
        PayoutRequest build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        /**
         * <p><em>Optional but recommended</em> A unique ID that you can include to prevent duplicating objects or transactions in the case that a request is sent more than once. This key isn't generated in Payabli, you must generate it yourself. This key persists for 2 minutes. After 2 minutes, you can reuse the key if needed.</p>
         */
        _FinalStage idempotencyKey(Optional<String> idempotencyKey);

        _FinalStage idempotencyKey(String idempotencyKey);

        /**
         * <p>When <code>true</code>, Payabli authorizes the payout for same-day ACH processing instead of standard ACH. Same-day ACH must be enabled for the paypoint, otherwise the authorization fails with a <code>400</code> response and <code>responseCode</code> <code>3492</code>. Only ACH payouts honor this flag. Wire and RTP payouts ignore it.</p>
         * <p>Because this endpoint captures immediately, pass <code>autoConvertSameDayAch</code> with a value of <code>true</code> to fall back to standard ACH if the capture runs after the same-day ACH cutoff.</p>
         */
        _FinalStage sameDayAch(Optional<Boolean> sameDayAch);

        _FinalStage sameDayAch(Boolean sameDayAch);

        /**
         * <p>When <code>true</code>, Payabli won't automatically create a bill for this payout transaction.</p>
         */
        _FinalStage doNotCreateBills(Optional<Boolean> doNotCreateBills);

        _FinalStage doNotCreateBills(Boolean doNotCreateBills);

        /**
         * <p>When <code>true</code>, the payout bypasses the requirement for unique bills, identified by vendor invoice number. This allows you to make more than one payout for a bill, like a split payment.</p>
         */
        _FinalStage allowDuplicatedBills(Optional<Boolean> allowDuplicatedBills);

        _FinalStage allowDuplicatedBills(Boolean allowDuplicatedBills);

        /**
         * <p>When <code>true</code>, Payabli updates the vendor's stored default payment method to the method used in this payout.</p>
         */
        _FinalStage updateVendorPaymentMethod(Optional<Boolean> updateVendorPaymentMethod);

        _FinalStage updateVendorPaymentMethod(Boolean updateVendorPaymentMethod);

        /**
         * <p>Controls what happens to a payout authorized with <code>sameDayACH</code> set to <code>true</code> when the capture runs after the same-day ACH cutoff. When <code>true</code>, Payabli converts the payout to a standard ACH payment and captures it. When <code>false</code>, the capture is declined.</p>
         * <p>This parameter has no effect on payouts that weren't authorized for same-day ACH.</p>
         */
        _FinalStage autoConvertSameDayAch(Optional<Boolean> autoConvertSameDayAch);

        _FinalStage autoConvertSameDayAch(Boolean autoConvertSameDayAch);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements BodyStage, _FinalStage {
        private AuthorizePayoutBody body;

        private Optional<Boolean> autoConvertSameDayAch = Optional.empty();

        private Optional<Boolean> updateVendorPaymentMethod = Optional.empty();

        private Optional<Boolean> allowDuplicatedBills = Optional.empty();

        private Optional<Boolean> doNotCreateBills = Optional.empty();

        private Optional<Boolean> sameDayAch = Optional.empty();

        private Optional<String> idempotencyKey = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(PayoutRequest other) {
            idempotencyKey(other.getIdempotencyKey());
            sameDayAch(other.getSameDayAch());
            doNotCreateBills(other.getDoNotCreateBills());
            allowDuplicatedBills(other.getAllowDuplicatedBills());
            updateVendorPaymentMethod(other.getUpdateVendorPaymentMethod());
            autoConvertSameDayAch(other.getAutoConvertSameDayAch());
            body(other.getBody());
            return this;
        }

        @java.lang.Override
        @JsonSetter("body")
        public _FinalStage body(@NotNull AuthorizePayoutBody body) {
            this.body = Objects.requireNonNull(body, "body must not be null");
            return this;
        }

        /**
         * <p>Controls what happens to a payout authorized with <code>sameDayACH</code> set to <code>true</code> when the capture runs after the same-day ACH cutoff. When <code>true</code>, Payabli converts the payout to a standard ACH payment and captures it. When <code>false</code>, the capture is declined.</p>
         * <p>This parameter has no effect on payouts that weren't authorized for same-day ACH.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage autoConvertSameDayAch(Boolean autoConvertSameDayAch) {
            this.autoConvertSameDayAch = Optional.ofNullable(autoConvertSameDayAch);
            return this;
        }

        /**
         * <p>Controls what happens to a payout authorized with <code>sameDayACH</code> set to <code>true</code> when the capture runs after the same-day ACH cutoff. When <code>true</code>, Payabli converts the payout to a standard ACH payment and captures it. When <code>false</code>, the capture is declined.</p>
         * <p>This parameter has no effect on payouts that weren't authorized for same-day ACH.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "autoConvertSameDayAch", nulls = Nulls.SKIP)
        public _FinalStage autoConvertSameDayAch(Optional<Boolean> autoConvertSameDayAch) {
            this.autoConvertSameDayAch = autoConvertSameDayAch;
            return this;
        }

        /**
         * <p>When <code>true</code>, Payabli updates the vendor's stored default payment method to the method used in this payout.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage updateVendorPaymentMethod(Boolean updateVendorPaymentMethod) {
            this.updateVendorPaymentMethod = Optional.ofNullable(updateVendorPaymentMethod);
            return this;
        }

        /**
         * <p>When <code>true</code>, Payabli updates the vendor's stored default payment method to the method used in this payout.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "updateVendorPaymentMethod", nulls = Nulls.SKIP)
        public _FinalStage updateVendorPaymentMethod(Optional<Boolean> updateVendorPaymentMethod) {
            this.updateVendorPaymentMethod = updateVendorPaymentMethod;
            return this;
        }

        /**
         * <p>When <code>true</code>, the payout bypasses the requirement for unique bills, identified by vendor invoice number. This allows you to make more than one payout for a bill, like a split payment.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage allowDuplicatedBills(Boolean allowDuplicatedBills) {
            this.allowDuplicatedBills = Optional.ofNullable(allowDuplicatedBills);
            return this;
        }

        /**
         * <p>When <code>true</code>, the payout bypasses the requirement for unique bills, identified by vendor invoice number. This allows you to make more than one payout for a bill, like a split payment.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "allowDuplicatedBills", nulls = Nulls.SKIP)
        public _FinalStage allowDuplicatedBills(Optional<Boolean> allowDuplicatedBills) {
            this.allowDuplicatedBills = allowDuplicatedBills;
            return this;
        }

        /**
         * <p>When <code>true</code>, Payabli won't automatically create a bill for this payout transaction.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage doNotCreateBills(Boolean doNotCreateBills) {
            this.doNotCreateBills = Optional.ofNullable(doNotCreateBills);
            return this;
        }

        /**
         * <p>When <code>true</code>, Payabli won't automatically create a bill for this payout transaction.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "doNotCreateBills", nulls = Nulls.SKIP)
        public _FinalStage doNotCreateBills(Optional<Boolean> doNotCreateBills) {
            this.doNotCreateBills = doNotCreateBills;
            return this;
        }

        /**
         * <p>When <code>true</code>, Payabli authorizes the payout for same-day ACH processing instead of standard ACH. Same-day ACH must be enabled for the paypoint, otherwise the authorization fails with a <code>400</code> response and <code>responseCode</code> <code>3492</code>. Only ACH payouts honor this flag. Wire and RTP payouts ignore it.</p>
         * <p>Because this endpoint captures immediately, pass <code>autoConvertSameDayAch</code> with a value of <code>true</code> to fall back to standard ACH if the capture runs after the same-day ACH cutoff.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage sameDayAch(Boolean sameDayAch) {
            this.sameDayAch = Optional.ofNullable(sameDayAch);
            return this;
        }

        /**
         * <p>When <code>true</code>, Payabli authorizes the payout for same-day ACH processing instead of standard ACH. Same-day ACH must be enabled for the paypoint, otherwise the authorization fails with a <code>400</code> response and <code>responseCode</code> <code>3492</code>. Only ACH payouts honor this flag. Wire and RTP payouts ignore it.</p>
         * <p>Because this endpoint captures immediately, pass <code>autoConvertSameDayAch</code> with a value of <code>true</code> to fall back to standard ACH if the capture runs after the same-day ACH cutoff.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "sameDayACH", nulls = Nulls.SKIP)
        public _FinalStage sameDayAch(Optional<Boolean> sameDayAch) {
            this.sameDayAch = sameDayAch;
            return this;
        }

        /**
         * <p><em>Optional but recommended</em> A unique ID that you can include to prevent duplicating objects or transactions in the case that a request is sent more than once. This key isn't generated in Payabli, you must generate it yourself. This key persists for 2 minutes. After 2 minutes, you can reuse the key if needed.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage idempotencyKey(String idempotencyKey) {
            this.idempotencyKey = Optional.ofNullable(idempotencyKey);
            return this;
        }

        /**
         * <p><em>Optional but recommended</em> A unique ID that you can include to prevent duplicating objects or transactions in the case that a request is sent more than once. This key isn't generated in Payabli, you must generate it yourself. This key persists for 2 minutes. After 2 minutes, you can reuse the key if needed.</p>
         */
        @java.lang.Override
        public _FinalStage idempotencyKey(Optional<String> idempotencyKey) {
            this.idempotencyKey = idempotencyKey;
            return this;
        }

        @java.lang.Override
        public PayoutRequest build() {
            return new PayoutRequest(
                    idempotencyKey,
                    sameDayAch,
                    doNotCreateBills,
                    allowDuplicatedBills,
                    updateVendorPaymentMethod,
                    autoConvertSameDayAch,
                    body,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
